# ESP-IDF Unit Test Configuration
# This is a test app that can be built separately

cmake_minimum_required(VERSION 3.16)

# Include ESP-IDF project
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define test project
project(hai_os_simplexl_test)
